/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote poster/backdrop domains (e.g. your object storage / CDN) here as they're set up.
    remotePatterns: [],
  },
};

module.exports = nextConfig;
